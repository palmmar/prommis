@{
    var isActive = ManageNavPages.IndexNavClass(ViewContext) == "active";
}

<nav class="grid gap-2">
    <a class="rounded-2xl border px-4 py-2 text-sm transition @((isActive) ? "border-ink bg-ink text-white dark:border-slate-100 dark:bg-slate-100 dark:text-slate-900" : "border-slate-200 text-slate-600 hover:border-slate-300 dark:border-slate-800 dark:text-slate-300 dark:hover:border-slate-600")" id="profile" asp-page="./Index">
        Profil
    </a>
</nav>
