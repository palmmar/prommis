@page
@model RegisterModel
@{
    ViewData["Title"] = "Skapa konto";
}

<div class="mx-auto grid max-w-5xl gap-8 lg:grid-cols-[1fr_1fr]">
    <div class="rounded-3xl border border-slate-200 bg-white p-8 shadow-sm dark:border-slate-800 dark:bg-slate-900/80">
        <p class="text-xs font-semibold uppercase tracking-[0.25em] text-slate-500 dark:text-slate-400">StegStat</p>
        <h1 class="mt-3 text-3xl font-semibold">Skapa ditt konto</h1>
        <p class="mt-2 text-slate-600 dark:text-slate-300">Samla steg, bygg grupper och följ framsteg över tid.</p>

        <form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="mt-6 grid gap-4">
            @if (!ViewData.ModelState.IsValid)
            {
                <div asp-validation-summary="ModelOnly" class="rounded-2xl border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-700 dark:border-rose-900/60 dark:bg-rose-950/40 dark:text-rose-200" role="alert"></div>
            }

            <div>
                <label asp-for="Input.Email" class="text-sm font-medium text-slate-700 dark:text-slate-300">E-post</label>
                <input asp-for="Input.Email" class="mt-2 w-full rounded-2xl border border-slate-300 bg-white px-4 py-2 text-slate-900 placeholder:text-slate-400 dark:border-slate-700 dark:bg-slate-900/70 dark:text-slate-100 dark:placeholder:text-slate-500" autocomplete="username" aria-required="true" placeholder="namn@exempel.se" />
                <span asp-validation-for="Input.Email" class="text-sm text-rose-600 dark:text-rose-300"></span>
            </div>

            <div>
                <label asp-for="Input.Password" class="text-sm font-medium text-slate-700 dark:text-slate-300">Lösenord</label>
                <input asp-for="Input.Password" class="mt-2 w-full rounded-2xl border border-slate-300 bg-white px-4 py-2 text-slate-900 placeholder:text-slate-400 dark:border-slate-700 dark:bg-slate-900/70 dark:text-slate-100 dark:placeholder:text-slate-500" autocomplete="new-password" aria-required="true" placeholder="Minst 6 tecken" />
                <span asp-validation-for="Input.Password" class="text-sm text-rose-600 dark:text-rose-300"></span>
            </div>

            <div>
                <label asp-for="Input.ConfirmPassword" class="text-sm font-medium text-slate-700 dark:text-slate-300">Bekräfta lösenord</label>
                <input asp-for="Input.ConfirmPassword" class="mt-2 w-full rounded-2xl border border-slate-300 bg-white px-4 py-2 text-slate-900 placeholder:text-slate-400 dark:border-slate-700 dark:bg-slate-900/70 dark:text-slate-100 dark:placeholder:text-slate-500" autocomplete="new-password" aria-required="true" placeholder="Bekräfta lösenord" />
                <span asp-validation-for="Input.ConfirmPassword" class="text-sm text-rose-600 dark:text-rose-300"></span>
            </div>

            <button id="registerSubmit" type="submit" class="mt-2 rounded-full bg-ink px-5 py-2 text-sm font-semibold text-white hover:bg-slate-800 dark:bg-slate-100 dark:text-slate-900 dark:hover:bg-white">Skapa konto</button>
        </form>

        <p class="mt-6 text-sm text-slate-600 dark:text-slate-300">
            Har du redan konto?
            <a class="text-accent hover:underline" asp-page="./Login" asp-route-returnUrl="@Model.ReturnUrl">Logga in</a>
        </p>
    </div>

    <div class="rounded-3xl border border-slate-200 bg-white p-8 shadow-sm dark:border-slate-800 dark:bg-slate-900/80">
        <h2 class="text-lg font-semibold">Trygg start</h2>
        <ul class="mt-4 grid gap-3 text-sm text-slate-600 dark:text-slate-300">
            <li class="rounded-2xl bg-mist p-4 dark:bg-slate-800/70">Håll koll på steg varje dag.</li>
            <li class="rounded-2xl bg-mist p-4 dark:bg-slate-800/70">Se utveckling per vecka, månad och år.</li>
            <li class="rounded-2xl bg-mist p-4 dark:bg-slate-800/70">Dela totalsummor med gruppen.</li>
        </ul>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
